--- src/components/ContactForm.tsx
+++ src/components/ContactForm.tsx
@@ -176,10 +176,17 @@
-        {errors.message && (
-          <p id="message-error" className="mt-1.5 text-xs text-red-400" role="alert" aria-live="polite">
-            {errors.message}
-          </p>
-        )}
+        <div className="mt-1.5 flex items-center justify-between">
+          {errors.message ? (
+            <p id="message-error" className="text-xs text-red-400" role="alert" aria-live="polite">
+              {errors.message}
+            </p>
+          ) : (
+            <div /> // Spacer
+          )}
+          <p className="text-[10px] uppercase tracking-widest text-text-muted" aria-hidden="true">
+            {data.message.length} / 2000
+          </p>
+        </div>
       </div>
